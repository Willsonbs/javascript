

/*var nome = window.prompt('Qual é o seu nome?');
        window.alert(`Seu nome possui ${nome.length} letras.`);
*/
       
var nome = window.prompt('Qual é o seu nome?');
        document.write(`Olá ${nome} seu nome possui ${nome.length} letras. <br/>`);
        document.write(`Seu em maiúsculo é ${nome.toUpperCase()} <br/>`);
        document.write(`Seu em minúsculo é ${nome.toLowerCase()} `);
         